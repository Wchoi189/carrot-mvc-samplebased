<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springapp.mvc.board.service.IBoardService">
    <select id="readBoardById" resultType="com.springapp.mvc.board.model.BoardDTO">
        select board_id, title, file_name, save_path, reg_date
        from board
        where board_id = #{board_id}
    </select>

    <select id="getBoardList" resultType="com.springapp.mvc.board.model.BoardDTO">
        select board_id, title, file_name, save_path, reg_date
        from board
    </select>

    <insert id="insertBoard">
        insert into board (title, file_name, save_path, reg_date)
        values (#{title}, #{file_name}, #{save_path}, #{reg_date})
    </insert>

    <delete id="deleteBoardById">
        delete from board
        where board_id = #{board_id}
    </delete>

    <update id="updateBoard">
        update board
        set title = #{title}, file_name = #{file_name}, save_path = #{save_path}, reg_date = #{reg_date}
        where board_id = #{board_id}
    </update>
</mapper>